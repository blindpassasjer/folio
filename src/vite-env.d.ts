/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Injected at build time by Vite's `define` (see vite.config.ts) from package.json.
declare const __APP_VERSION__: string;
